# Copy to `.env.local` for local development (overrides `.env`). Do not commit `.env.local`.
NEXT_PUBLIC_SITE_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:3001

# Optional: proxy /api to remote while on localhost
# NEXT_PUBLIC_DEV_API_PROXY_TARGET=https://jamspace.corexinnovations.com
